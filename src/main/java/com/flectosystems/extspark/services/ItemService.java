package com.flectosystems.extspark.services;

import spark.servlet.SparkApplication;

public class ItemService implements SparkApplication {

    @Override
    public void init() {

        // around here go the diff integration (WS) methods
    }
}